/**
 * Centralized utilities for OGameX
 */

// Formatting utilities
export {
  formatNumber,
  formatNumberFull,
  formatCoordinates,
  formatCoordinatesObj,
  formatTimeRemaining,
  formatTimeAgo,
  formatDuration,
  formatDate,
  formatDateTime,
  formatServerTime,
  formatPercentage,
  formatResource,
  formatDistanceToNow,
} from './format'
